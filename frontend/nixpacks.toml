[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[variables]
REACT_APP_API_URL = "https://taurus-logistics-production.up.railway.app/api"
CI = "false"
GENERATE_SOURCEMAP = "false"
SKIP_PREFLIGHT_CHECK = "true"

[start]
cmd = "npx serve -s build -p ${PORT:-3000}"
