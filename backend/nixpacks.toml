[phases.setup]
nixPkgs = ["python311", "libmysqlclient"]

[phases.build]
cmds = ["pip install -r requirements.txt", "python manage.py migrate"]

[start]
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
